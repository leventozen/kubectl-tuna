apiVersion: v1
kind: Pod
metadata:
  name: notifier-8f6b2-k3n9v
  namespace: messaging
  labels:
    app: notifier
  ownerReferences:
    - apiVersion: apps/v1
      kind: ReplicaSet
      name: notifier-8f6b2
      uid: rs-uid-5
      controller: true
spec:
  containers:
    - name: app
      image: notifier:0.9
      envFrom:
        - configMapRef:
            name: notifier-config
status:
  phase: Pending
  qosClass: BestEffort
  conditions:
    - type: PodScheduled
      status: "True"
    - type: Ready
      status: "False"
      reason: ContainersNotReady
      message: "containers with unready status: [app]"
  containerStatuses:
    - name: app
      ready: false
      restartCount: 0
      image: notifier:0.9
      imageID: ""
      containerID: ""
      state:
        waiting:
          reason: CreateContainerConfigError
          message: configmap "notifier-config" not found
